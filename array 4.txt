pragma solidity >=0.8.2 <0.9.0;
import "hardhat/console.sol";
contract Storage {
 uint[] myarray=[1,2,3,5,6];
 function getArraySlice() public view returns (uint[] memory) {
 for (uint i =0; 1< myarray.length; i++) 
 {
 console.log("Array element",myarray[i]);
 }
 }
}